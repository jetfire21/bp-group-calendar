customization by Alexey S.
ver.2
для секции event tasks & shifts используется уже 2 таблицы bp_time, bp_tasks в бд,в которую двумерный динамический массив с данными попадает в виде строки, но не в сериализованном виде,эта архитектура гораздо лучше предыдущей
---------
ver.1
для секции event tasks & shifts используется только одна таблица bp_group_bgc_tasks в бд,в которую двумерный динамический массив с данными попадает в сериализованном виде
это не совершенная архитектура,есть ограничения..трудно перебрать значения динамического массива чтобы вывести и редактировать эти данные
использовался чисто для теста